import { css } from '@emotion/react';

export const inputStyle = css({
  padding: 0,
  display: 'none',
});

export const checkboxStyle = css({
  '& > svg': {
    width: '28px',
    height: '28px',
  },
});
